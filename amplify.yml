version: 1
applications:
  - appRoot: frontend
    frontend:
      phases:
        preBuild:
          commands:
            # 🧹 Ensure clean install for all dependencies
            - npm ci || npm install --legacy-peer-deps

            # 🧠 Show environment info (optional for debugging)
            - node -v
            - npm -v

        build:
          commands:
            # ⚙️ Run the Vite production build
            - npm run build

      artifacts:
        # 📦 Output folder for Vite build
        baseDirectory: dist
        files:
          - '**/*'

      cache:
        # ⚡ Cache dependencies for faster rebuilds
        paths:
          - node_modules/**/*

    environment:
      variables:
        # 🌍 Environment variables available during build
        VITE_API_BASE: https://n5dczw3bzj.execute-api.eu-west-2.amazonaws.com
        VITE_GIPHY_API_KEY: N1ChWoOAZj7IhtH39lm75
        NODE_ENV: production
