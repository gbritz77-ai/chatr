version: 1
applications:
  - appRoot: frontend
    frontend:
      phases:
        preBuild:
          commands:
            - echo "🧭 preBuild PWD:"
            - pwd
            - ls -la
            - nvm install 20
            - nvm use 20
            - npm ci
            - npm install -g @aws-amplify/cli
            - |
              echo "⚙️ amplify pull (dev)"
              amplify pull --yes --env dev \
              --amplify '{ "envName": "dev" }' \
              --frontend '{ "frontend": "javascript", "framework": "none", "config": {} }' \
              --providers '{ "awscloudformation": { "configLevel": "project" } }'
        build:
          commands:
            - echo "🚀 build PWD:"
            - pwd
            - ls -la
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
