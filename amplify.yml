version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    # This tells Amplify to find your build files in src/dist
    baseDirectory: src/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*