version: 1
applications:
  - appRoot: frontend
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci || npm install --legacy-peer-deps
            - node -v
            - npm -v
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
      customRules:
        # ✅ This ensures /login, /register, etc. load index.html (SPA routing)
        - source: </*>
          target: /index.html
          status: 200
    environment:
      variables:
        VITE_API_BASE: https://6ideo0rlff.execute-api.eu-west-2.amazonaws.com/dev
        VITE_GIPHY_API_KEY: N1ChWoOAZj7XVelZKNlsCfIhtH39lm75
        NODE_ENV: production
